# Final packages
RPMS/*

# Binaries somehow created in the project folder (we should try shadow building to get rid of them in the project folder)
# And I don't really know why sailfish-app-with-qml-test is generated two times
sailfish-app-with-qml-test
src/sailfish-app-with-qml-test
tests/tst-sailfish-app-with-qml-test

# Something Qt Creator generates
debugsources.list
documentation.list
debugfiles.list
debuglinks.list

# object files, makefiles, environment-specific Creator settings
*.o
Makefile
*.pro.user